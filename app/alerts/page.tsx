"use client";import{useEffect,useState}from'react';
export default function Alerts(){const[a,setA]=useState<any[]>([]);const[c,setC]=useState<any>({});async function load(){const r=await fetch('/api/admin/alerts-smart',{cache:'no-store'});const j=await r.json();setA(j.data||[]);setC(j.settings||{})}useEffect(()=>{void load()},[]);async function read(id:number){await fetch('/api/admin/alerts-smart',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'read',id})});void load()}async function save(){await fetch('/api/admin/alerts-smart',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(c)});void load()}return <main dir="rtl" className="admin-page space-y-6"><section className="admin-hero"><div><span>SMART ALERTS</span><h1>مركز التنبيهات الذكي 🔔</h1><p>تنبيهات المخزون والحالات المهمة بدون إزعاج زائد.</p></div><div className="admin-hero-orb">🔔</div></section><section className="admin-card"><div className="flex flex-wrap items-center justify-between gap-3"><h2>قواعد التنبيه</h2><button onClick={save} className="admin-action">حفظ القواعد</button></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><label className="rounded-2xl bg-surface p-4 text-xs font-bold"><input type="checkbox" checked={c.low_stock_enabled!==false} onChange={e=>setC({...c,low_stock_enabled:e.target.checked})}/> تنبيه المخزون</label><label className="rounded-2xl bg-surface p-4 text-xs font-bold">الحد الأدنى<input className="mt-2" type="number" min="0" value={c.low_stock_threshold??2} onChange={e=>setC({...c,low_stock_threshold:e.target.value})}/></label><label className="rounded-2xl bg-surface p-4 text-xs font-bold"><input type="checkbox" checked={c.profit_drop_enabled!==false} onChange={e=>setC({...c,profit_drop_enabled:e.target.checked})}/> مراقبة الربح</label><label className="rounded-2xl bg-surface p-4 text-xs font-bold"><input type="checkbox" checked={c.large_discount_enabled!==false} onChange={e=>setC({...c,large_discount_enabled:e.target.checked})}/> خصومات كبيرة <input className="mt-2" type="number" min="0" value={c.large_discount_percent??30} onChange={e=>setC({...c,large_discount_percent:e.target.value})}/></label></div></section><section className="admin-card"><div className="flex items-center justify-between"><h2>التنبيهات</h2><button onClick={async()=>{await fetch('/api/admin/alerts-smart',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'all_read'})});void load()}} className="admin-action">تحديد الكل كمقروء</button></div><div className="mt-4 space-y-2">{a.map(x=><button key={x.id} onClick={()=>void read(x.id)} className={`w-full rounded-2xl border p-4 text-right ${x.is_read?'border-line bg-white':'border-blue/20 bg-blue/5'}`}><div className="flex items-start justify-between gap-3"><div><b>{x.title}</b><p className="mt-1 text-xs text-ink-soft">{x.body}</p></div><span className="text-[10px] font-bold">{new Date(x.created_at).toLocaleString('ar-SY')}</span></div></button>)}{!a.length&&<p className="text-sm text-ink-soft">ما في تنبيهات حالياً.</p>}</div></section></main>}
